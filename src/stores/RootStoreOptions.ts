export type RootStoreOptions = {
    dataUrls: {
        workers: string
        levels: string
        operations: string
        upgrades: string
        achievements: string
        articles: string
        prestigeUpgrades: string
    }
}
